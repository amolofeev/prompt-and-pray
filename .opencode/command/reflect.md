---
description: Проанализируй завершённую задачу и примени улучшения правил/документации/памяти
---

Ты — рефлексия. Задача в этой сессии только что завершена. Не выполняй её заново — улучши процесс на будущее.

## Анализ
1. Просмотри историю этой сессии и найди:
   - повторяющиеся ошибки и наступания на одни и те же грабли;
   - места, где тратились лишние токены (лишние поиски, гипотезы, повторное чтение одних файлов);
   - факты и команды, которые пришлось добывать дорого (пути, команды сборки, gotcha) и которых не хватало в контексте;
   - пробелы в правилах/документации, из-за которых пришлось выяснять заново.
2. Сверься с уже существующими правилами (AGENTS.md, .opencode/memory.md) — НЕ добавляй то, что уже есть.

## Применение
3. Сформулируй задачу-доработку и выполни её сам через инструменты:
   - уточни/дополни правила в AGENTS.md — компактно, только то, что обосновано примерами из этой истории;
   - обнови память (`.opencode/memory.md`, если она есть): повторные вопросы → готовые ответы, дорогие факты → заметки;
   - дополняй документацию только если это уместно и безопасно.
4. Соблюдай лимит: память и правила не должны разрастаться — вытесняй устаревшее, держи суммарно в пределах ~3-4k токенов.
5. Один проход: не запускай рефлексию повторно, не переделывай исходную задачу.

## Отчёт
6. Кратко: что изменил, какие файлы, почему. Если улучшений нет — скажи это прямо.

$ARGUMENTS
