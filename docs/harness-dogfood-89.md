# Верификация контура после пересборки #89: «дерево рекурсивного делегирования»

Отчёт по национальной верификации (вершина #99) переупаковки харнеса с
delivery-led «звезды» на «дерево рекурсивного делегирования с агрегацией
наверх» (задача #89, сабтаски #90–#98). Правок контура по итогам проверки не
потребовалось — артефакт фиксирует результат проверки.

## Проверенные инварианты (постановка #89)

| # | Проверка | Результат |
|---|---|---|
| 1 | Синхронность реестра: матрица «узел → дети» в `docs/harness-agents.md` совпадает с `permission.task` в frontmatter всех 9 ролей | PASS: совпадают (delivery/BA/SA/team-lead-*/developer-*/specificator) |
| 2 | Ацикличность и запрет самовызова (A2.2): транзитивное замыкание узла не достигает его самого; `delivery` — 0 входящих рёбер от узлов; `specificator` и `developer-*` — 0 исходящих | PASS: граф ацикличен, самовызовов нет |
| 3 | `opencode agent list` поднимает все роли, включая hidden `specificator`; конфиг валиден (A5.2) | PASS: 9 ролей харнеса + explore/general |
| 4 | YAML-контракты (route/requirements/specification/plan/done) не изменены; Node contract и `aggregate` — аддитивные (A1.2, A7.1) | PASS: формы прежние, изменения только аддитивные |
| 5 | `tasks-gh` не изменён (A5.4): регрессия графа `git log -- .opencode/skills/tasks-gh/SKILL.md` | PASS: последний коммит caadbaa (#85), вне скоупа #89 |
| 6 | Smoke: паттерн узла-оркестратора и контракт Specificator описаны в спеке и скилле `harness-workflow`; AGENTS.md содержит «Модель исполнения» | PASS |
| 7 | [AI]-форматы: все закрытые листья #90–#98 имеют [AI]-close-комментарии | PASS |

## Итог

Критерии A2.2, A3.1–A3.2, A4.1–A4.3, A5.1–A5.4, A7.1–A7.2 постановки #89
выполнены. Изменён конфиг opencode (роли, скилл, спека, манифест) — требуется
перезапуск opencode для вступления в силу.